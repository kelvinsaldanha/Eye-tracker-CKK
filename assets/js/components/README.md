# Componentes

Os componentes reutilizáveis do site são gerados em `assets/js/main.js` por funções como `sectionShell`, `cardGrid`, `downloadButton` e renderizadores de seções.

Esta pasta fica reservada para separar componentes no futuro caso o projeto cresça.
